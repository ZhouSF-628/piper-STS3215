[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.backends._legacy:_Backend"

[project]
name = "piper-sts3215"
version = "0.1.0"
description = "Piper 外骨骼 & STS3215 舵机遥操系统"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
"*" = ["README.md"]
